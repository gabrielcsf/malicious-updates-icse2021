"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function getFractionalLength(number) {
    /**
     * Match numbers like 1.123 and and 1e-7 and 1.5e-3 and 1.25e+10
     */
    var match = String(Number(number)).match(/(?:\.(\d+))?(?:[eE]([+-]?\d+))?$/);
    if (!match) {
        return 0;
    }
    var digitsAfterDot = match[1] ? match[1].length : 0;
    var powerOfTen = match[2] ? Number(match[2]) : 0;
    return Math.max(0, digitsAfterDot - powerOfTen);
}
exports.getFractionalLength = getFractionalLength;
//# sourceMappingURL=getFractionalLength.js.map